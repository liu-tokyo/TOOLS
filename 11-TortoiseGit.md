



## 解决CPU和磁盘资源消耗

> <FONT COLOR=BLUE>**使用【方案二】，感觉资源消耗情况就得到了很大缓解。**</FONT>

打开 Git 库文件存放目录的时候，非常消耗 CPU 及磁盘资源，在 Windows 上使用 TortoiseGit（乌龟Git）时非常普遍。之所以会这样，是因为 TortoiseGit 默认开启了图标覆盖（Icon Overlays）功能。

为了在资源管理器里实时显示文件和文件夹的 Git 状态（比如绿色的勾表示未修改，红色的感叹号表示已修改），TortoiseGit 后台会启动一个名为 `TSVNCache.exe` 的守护进程。每当你点击或打开一个包含 Git 库的目录，这个进程就会**疯狂地扫描和遍历该目录下的所有文件以及 `.git` 文件夹里的索引**，从而导致 CPU 和磁盘（或 SSD）使用率瞬间飙升，引发资源管理器卡顿。

要解决或极大地缓解这个性能问题，可以通过以下几种方法对 TortoiseGit 进行优化：

### 方案一：修改状态缓存模式（最推荐，兼顾性能与实用性）

你可以让 TortoiseGit 不要那么频繁、那么深地去扫描文件。

1. 在桌面或任意文件夹空白处**右键** → 选择 **TortoiseGit** → 点击 **Settings (设置)**。
2. 在左侧导航栏找到 **Icon Overlays (图标覆盖)**。
3. 在右侧的 **Status cache (状态缓存)** 选项中，你会看到几个选项，根据需求调整：
   - **Default (默认)：** 默认值，最消耗资源。它会递归扫描子目录。
   - **Shell：** 由 Windows Shell 直接处理。**推荐改为此项**，它只在资源管理器请求时才计算状态，能大幅降低后台持续扫描的开销。
   - **None (无)：** 彻底关闭状态缓存（图标可能不再实时变色，但完全不占资源）。

### 方案二：限制图标覆盖的范围（精准避让）

如果你电脑里有很多非 Git 目录（或者某些 Git 仓库文件极其庞大），可以让 TortoiseGit 彻底无视它们。

1. 同样在 **Icon Overlays (图标覆盖)** 设置页面。
2. 找到下方两个关键的输入框：
   - **Exclude paths (排除路径)：** 在这里填入你不想让乌龟Git扫描的盘符或大文件夹。例如输入 `C:\*` 或 `D:\LargeProjects\*`。
   - **Include paths (包含路径)：** 在这里只填入你存放 Git 代码库的专用根目录。例如 `D:\GitProjects\*`。
3. **这样设置后，TortoiseGit 就只会死盯着你的代码目录，绝对不去碰电脑里的其他文件，能释放大量资源。**

### 方案三：关闭不需要的图标状态（精简开销）

默认情况下，TortoiseGit 会针对“未版本化”、“被忽略”等多种状态都去计算并显示图标。

1. 在 **Icon Overlays** → **Overlay Handlers** 中。
2. **取消勾选** 那些你不需要的图标类型。比如取消勾选 `Unversioned (未版本化)`、`Ignored (已忽略)`、`Locked (已锁定)`。
3. 只要勾选最核心的 `Normal (正常)` 和 `Modified (已修改)` 即可，减少它的计算量。

### 终极一招：关闭 `TSVNCache.exe`（追求极致流畅）

如果你平时习惯看右键菜单里的状态，或者直接配合 VS Code、Gitkraken 等其他工具看状态，不需要资源管理器图标变色：

1. 在 **Status cache (状态缓存)** 中直接选择 **None**。
2. 打开 Windows 任务管理器，手动结束掉 `TSVNCache.exe` 进程（或者重启电脑）。

这样，资源管理器在打开 Git 目录时就会像打开普通文件夹一样秒开，CPU 和磁盘读写会瞬间恢复平静。

你可以先尝试**方案一（改为 Shell 模式）\**加\**方案二（限制包含路径）**，这通常能在保留图标功能的同时，让卡顿感彻底消失。