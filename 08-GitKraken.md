# GitKraken

## 一、功能介绍

**GitKraken** 是一款非常著名且高颜值的 **Git 版本控制图形化客户端（GUI）**。

简单来说，如果你在开发项目时需要使用 Git 来管理代码版本（比如提交、分支管理、合并代码），但又觉得在命令行（Terminal）里敲 `git commit`、`git merge`、`git rebase` 这些命令太枯燥、容易出错、不够直观，那么 GitKraken 就是用来解救你的工具。

它把复杂的 Git 命令行操作，全部变成了**可视化的图表、拖拽操作和点击按钮**。

### 核心主打功能

### 1. 极具视觉冲击力的“分支树”（Commit Graph）

这是 GitKraken 最出名的功能。它用非常清晰、彩色的线条把项目的每一次提交（Commit）、每一个分支（Branch）的分化和合并过程画成了一张图。

- 你能一眼看清谁在什么时候改了哪行代码，哪个分支是从哪里分出来的。
- 哪怕项目有几十个人协作、上百个分支，它也能理得清清楚楚。

### 2. 拖拽式操作（Drag and Drop）

在命令行里合并分支可能需要敲好几行命令，但在 GitKraken 里：

- 你只需要用鼠标选中一个分支，**拖拽**到另一个分支上，就能直接进行 `Merge`（合并）或 `Rebase`（变基）。
- 想要切换分支？直接双击对应的分支名即可。

### 3. 强大的冲突解决工具（Merge Tool）

多人协同开发时，最头疼的就是“代码冲突（Conflict）”。GitKraken 内置了一个非常直观的三栏式冲突编辑器：

- 左边是你的修改，右边 low 别人的修改，中间是合并后的最终结果。
- 你只需要勾选方框，就能决定保留哪一部分代码，再也不用在代码文件里人肉寻找 `<<<<<<< HEAD` 这种让人崩溃的标记了。

### 4. 深度集成主流代码托管平台

它能无缝连接 **GitHub、GitLab、Bitbucket、Azure DevOps** 等平台。

- 你可以直接在 GitKraken 内部创建、评审、批准 **Pull Request (PR)** 或 **Merge Request (MR)**，不需要频繁在浏览器和本地软件之间切换。

### 它与 PyCharm 内置 Git 工具的区别

既然你之前了解过 PyCharm，可能会问：“PyCharm 里面不是已经自带 Git 视觉化工具了吗？为什么还要单独用 GitKraken？”

- **PyCharm 的 Git 工具：** 属于“IDE 附属功能”。它很方便，适合你在写代码的中间，顺手做个简单的提交或拉取。
- **GitKraken：** 属于“术业有专攻”。它是专业的独立 Git 管理软件。当面对**极其复杂的历史冲突、需要频繁跨项目管理 Git、或者团队多人协作分支满天飞**的情况时，GitKraken 的操作流畅度、分支图的清晰度以及冲突解决能力，会比 PyCharm 更加专业和强大。
- **跨语言/跨编辑器：** 如果你今天用 PyCharm 写 Python，明天用 VS Code 写前端，后天用 Xcode 写 iOS，你不需要去熟悉每个软件自带的 Git 界面，直接用一个 GitKraken 就能接管你电脑里所有的项目。

> **💰 关于资费：** GitKraken 针对**公开的开源项目（Public Repositories）是免费的**。但如果你要管理公司或个人的私有仓库（Private Repositories），则需要购买它的 Pro 商业版订阅（不过它对学生和教师同样有免费优惠政策）。



## 二、和 TortoiseGit 的区别

**GitKraken** 和 **TortoiseGit（俗称“小乌龟”）** 都是为了让开发者摆脱命令行、通过图形界面管理 Git 的工具。但它们的**设计理念、工作习惯、底层架构和目标人群**有着巨大的代差。

如果用一个通俗的比喻：

- **TortoiseGit** 就像是 Windows 系统里自带的经典功能组件，朴实无华、深度集成，老程序员用起来特别顺手。
- **GitKraken** 则像是极具现代感的电子竞技仪表盘，酷炫、直观、把所有信息流动都变成了动感图表。

下面是它们最核心的几个区别：

### 1. 交互方式与工作流

- **GitKraken（独立应用 / 一体化）：** 它是一个完全独立的软件。所有的操作（看分支树、写代码对比、处理冲突、提 Pull Request）全部都在这一个精致的暗黑模式窗口里完成。它拥有强大的**拖拽功能**，比如把分支 A 拖到分支 B 上就能直接弹出合并选项。
- **TortoiseGit（右键菜单集成 / 弹窗式）：** 它没有一个常驻的“主大厅”。它是深度嵌入到 Windows 资源管理器（File Explorer）中的。
  - 你想提交代码？在项目文件夹空白处**点右键** $\rightarrow$ 选择 `Git Commit`，这时会弹出一个新窗口。
  - 你想看历史记录？再**点右键** $\rightarrow$ 选择 `TortoiseGit` $\rightarrow$ `Show Log`，又会弹出一个窗口。
  - 它的图标会直接覆盖在你的系统文件夹上（绿勾表示未修改，红感叹号表示有改动），让你不打开任何软件就能知道文件状态。

### 2. 跨平台支持（操作系统）

- **TortoiseGit：仅支持 Windows。** 因为它的核心卖点就是跟 Windows 的 Shell（右键菜单、文件图标覆盖）深度绑定，所以它无法在 macOS 或 Linux 上运行。
- **GitKraken：全平台通用。** 它基于 Electron 架构开发，在 Windows、macOS 和 Linux 上的界面和体验完全一模一样。如果你平时需要在不同的操作系统间切换，GitKraken 能提供高度一致的习惯。

### 3. 分支图的清晰度（Commit Graph）

- **GitKraken：** 它的分支树是动态、彩色且带有头像的。多人协作时，由于主线和支线用不同颜色延伸，且支持实时平滑滚动，在几百个分支的复杂大项目中，它依然能让你清晰地看到“代码是怎么流动的”。
- **TortoiseGit：** 它的 `Show Log` 分支图相对比较传统和严谨，线条较细，更像是一个带有连线的表格。虽然信息很全（包括各种 Tag 和 Ref），但当分支极多时，视觉上的杂乱感会比 GitKraken 明显。

### 4. 费用与授权

- **TortoiseGit：完全免费且开源。** 它是基于 GPL 协议的开源软件，无论你是个人用、公司用，管理公开仓库还是商业私有仓库，都**不需要花一分钱**，没有任何功能限制。
- **GitKraken：商业收费（带免费版）。** 它管理公开的开源仓库是免费的。但是，如果你想用它来管理公司或者个人的**私有仓库（Private Repo）**，就需要购买它的专业版（Pro）订阅。

### 对比总结表

| **特性**     | **GitKraken**                      | **TortoiseGit (小乌龟)**                |
| ------------ | ---------------------------------- | --------------------------------------- |
| **软件形态** | 独立独立的现代化 App               | 嵌入 Windows 系统的右键菜单组件         |
| **支持系统** | Windows, macOS, Linux              | **仅限 Windows**                        |
| **操作特色** | 鼠标拖拽合并、可视化三栏冲突解决   | 传统的右键点击、弹窗处理                |
| **平台集成** | 内部直接查看 GitHub/GitLab 的 PR   | 需要跳转到浏览器处理 PR                 |
| **资源占用** | 相对较高（基于 Electron 较吃内存） | 极低（原生 Windows C++ 开发，非常轻量） |
| **费用**     | 私有仓库需付费订阅                 | **完全免费且开源**                      |

### 我该怎么选？

- **选择 TortoiseGit 的理由：** 如果你使用的是 Windows 系统，习惯了在文件夹里直接右键操作，不需要经常跨平台，且团队项目私有、不想支付软件订阅费，小乌龟是多年验证下来极其稳定、省心的经典之选（国内很多从早期 SVN/TortoiseSVN 转过来的开发者对它情有独钟）。
- **选择 GitKraken 的理由：** 如果你喜欢现代化的暗黑界面，经常需要处理复杂的分支合并与代码冲突，或者在使用 Mac/Linux 系统，并且追求极高效率的视觉化操作（比如直接在软件里把 GitHub 的 PR 拖下来处理），那么 GitKraken 会带来非常爽快的使用体验。