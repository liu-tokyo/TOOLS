# TablacusExplorer

 **Tablacus Explorer（简称 TE）**。作为一款在高级 Windows 用户圈子里非常有名的开源文件管理器，它简直就是为“折腾流”和“极简主义”量身定制的。

结合你之前对系统优化和效率工具的关注，这款软件非常有看头。我们可以从**为什么它很强**以及**怎么用好它**来聊聊：

## 核心特质：它为什么是神机？

1. **极度轻量与纯绿色（单文件级）：** 它的体积只有不到 1MB（解压后也就几兆），不需要安装，解压即用。它不像别的大型文件管理器那样臃肿，对内存和 CPU 的占用低到可以忽略不计，非常适合常驻后台。
2. **恐怖的“乐高式”扩展性（Add-ons）：** 原生状态下的 TE 极其简陋，甚至连个多标签页都没有。但它好玩的地方在于**完全由插件驱动**。你想要什么功能，就去插件商店里点一下“安装”。不需要的功能一个都不多占资源。
3. **支持多语言和多内核：** 它是日本开发者 Gaku 开发的，原生对中文、日语等支持非常好。而且它目前正在逐步从老旧的 IE 内核向基于 Edge 的 Blink 内核（WebView2）过渡，性能和现代化渲染能力越来越强。

## 🛠️ 推荐给你的必备插件（Add-ons）组合

如果你第一次用，建议在选项里的“获取扩展”中直接搜索并安装以下几个插件，几分钟就能把它组装成一个生产力怪兽：

- **基础布局：**
  - `Tabs`（多标签页）：让文件管理器像浏览器一样开多个标签。
  - `Split`（分栏）：可以实现双栏（像 Total Commander 那样）甚至四栏、九栏布局，双栏拖拽复制文件效率极高。
  - `Tree view`（树状目录）：在左侧显示传统的目录树。
    `工具` → `附件组件` → 输入 `tree` 搜索，`Tree view` 会出现在左侧的列表中，点击它，右侧点击该组件的`设置`按钮，勾选 `目录自动展开`。
- **效率增强：**
  - `Mouse gestures`（鼠标手势）：用鼠标右键划一下就能实现“返回上一层”、“关闭标签”等操作。
  - `Filter bar`（过滤栏）：在角落加一个输入框，输入几个字母就能瞬间过滤当前文件夹下的特定文件，找代码或者找特定配置非常爽。
  - `Address bar`（地址栏）：恢复传统的地址栏路径显示。

## 💡 针对现代系统（Windows 11）的避坑与高级技巧

因为 TE 的底层架构比较特殊（大量使用 JavaScript 脚本控制），在最新的 Windows 11 环境下使用时，有两点需要注意：

> **⚠️ Windows 11 复制卡死问题（2025/2026年已知 Bug）：** 最近在 Win11 系统中，有用户反馈在使用 TE 进行第二次或多次文件复制时，进度条可能不弹出来甚至导致操作卡住。 **【解决方法】：** 只要在 TE 的插件管理里，**禁用（Disable）“Multi-thread（多线程）”** 插件，然后搜索并**安装“Multi-process（多进程）”** 插件，就可以完美绕过这个系统兼容问题。

> **🔒 配置文件不要放 Program Files：** 建议把 TE 解压在非系统盘（比如 `D:\Tools\Tablacus`），这样它的配置文件会直接存在当前目录下的 `config` 文件夹里，变成纯绿色版，重装系统或者换电脑直接把整个文件夹拷走就能用，所有的标签、布局设定全部都在。